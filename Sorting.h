#include <iostream>
#include <string>
#include <random>
#include <cstdlib>
#include <ctime>


// Bubble Sort
extern void bubbleSort(int array[], int size);

// Insertion Sort
extern void insertionSort(int array[], int size);

// Merge Sort
extern void mergeSort(int array[], int size);

extern void _mergeSort(int array[], int first, int last);

extern void merge(int array[], int first, int mid, int last);

// Quick Sort
extern void quickSort(int array[], int size);

extern void _quickSort(int array[], int first, int last);

// Radix Sort (Strings)
extern void radixSort(int array[], int size);

